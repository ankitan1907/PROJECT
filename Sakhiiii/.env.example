# Sakhi Women Safety App - Environment Configuration
# Copy this file to .env and fill in your actual values

# Backend Configuration
VITE_BACKEND_URL=http://localhost:3000
# For production: VITE_BACKEND_URL=https://your-api-domain.com

# Google Services Configuration
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id_here

# Backend Environment Variables (for server/.env)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/sakhi-db
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890
PORT=3000

# Security Configuration
NODE_ENV=production
JWT_SECRET=your_jwt_secret_key_here

# CORS Configuration
ALLOWED_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com

# Deployment Configuration
# Netlify: Set these in Netlify dashboard under Site settings > Environment variables
# Vercel: Set these in Vercel dashboard under Project settings > Environment variables
# Heroku: Set these using: heroku config:set VARIABLE_NAME=value

# Required for production deployment:
# 1. VITE_BACKEND_URL - Your deployed backend URL
# 2. VITE_GOOGLE_MAPS_API_KEY - Google Maps API key with necessary permissions
# 3. MONGODB_URI - MongoDB Atlas connection string
# 4. TWILIO credentials - For SMS functionality

# Optional:
# 5. VITE_GOOGLE_CLIENT_ID - For Google OAuth login
